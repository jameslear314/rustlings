// variables3.rs
//
// Execute `rustlings hint variables3` or use the `hint` watch subcommand for a
// hint.

fn main() {
    let x: i32;
    x = 20; // Must initialize with a value fitting the type
    println!("Number {}", x);
}
