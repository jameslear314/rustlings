// variables1.rs
//
// Make me compile!
//
// Execute `rustlings hint variables1` or use the `hint` watch subcommand for a
// hint.

// What did we learn? In rust, you must 'let' a variable be what it is.

fn main() {
    let x = 5;
    println!("x has the value {}", x);
}
